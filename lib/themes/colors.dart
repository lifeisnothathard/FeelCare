import 'package:flutter/material.dart';

// Defines custom colors for the application.
class AppColors {
  // Light green for primary backgrounds, app bar, etc.
  static const Color primaryGreen = Color(0xFFE8F5E9);
  // Darker green for accents, selected text, button backgrounds.
  static const Color darkGreen = Color(0xFF4CAF50);
  // Off-white/light green for card backgrounds.
  static const Color cardBackground = Color(0xFFF1F8E9);
  // Dark gray for general text.
  static const Color textColor = Color(0xFF424242);
  static const Color backgroundColor = Color.fromARGB(255, 233, 207, 228);
}
